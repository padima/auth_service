[package]
name = "auth_service"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "^0.8.9"
tokio = { version = "^1", features = ["full"] }
hyper = "^0.14"
serde = { version = "^1", features = ["derive"] }
serde_json = "^1.0"
dotenv = "^0.15.0"
jsonwebtoken = "^9"
chrono = "^0.4"

[profile.dev]
opt-level = 0
debug = true