[package]
name = "auth_service"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
axum = "^0.8.9"
tokio = { version = "^1", features = ["full"] }
serde = { version = "^1", features = ["derive"] }
serde_json = "^1.0"
dotenv = "^0.15.0"
jsonwebtoken = "^9"
chrono = "^0.4"
